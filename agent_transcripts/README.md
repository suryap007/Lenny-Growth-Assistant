# Agent Transcripts

This directory contains coding-agent logs for evaluation purposes.
- `successful_run.md`: Examples of a successful LLM generation sequence.
- `failed_attempt_example.md`: Examples of what to avoid or how the system gracefully handles failure.
