# Failed Attempt

**User**: Make me an artifact of an interactive 3D game using Three.js

**System (Router)**: Selected `artifact` skill.

**Assistant Output**:
```json
{
  "artifact_type": "html",
  "artifact_title": "3D Game",
  "artifact_content": "<!-- Fails constraint: No JavaScript frameworks inside generated HTML -->"
}
```

*Note: The app avoids injecting external CDNs or complex JS, so this request correctly fails or falls back to plain HTML/CSS approximations depending on LLM alignment.*
